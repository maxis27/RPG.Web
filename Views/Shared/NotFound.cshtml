@{
    ViewBag.Title = "Nie znaleziono";
}

<div class="card">
    <div class="card-header">@ViewBag.Title</div>
    <div class="card-body">
        <h5 class="card-title">@ViewBag.ErrorMessage</h5>
        <a asp-controller="Home" asp-action="Index" class="btn btn-info">Powrót do strony głównej</a>
    </div>
</div>